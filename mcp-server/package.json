{
  "name": "embed-generator-mcp",
  "version": "0.1.0",
  "description": "MCP server for Embed & Image Generator — markdown to PNG for agents",
  "license": "MIT",
  "type": "module",
  "bin": {
    "embed-generator-mcp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=18"
  }
}
